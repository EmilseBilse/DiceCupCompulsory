<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dicecupcompulsory">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DiceCupCompulsory">
        <activity
            android:name=".dice.DiceActivity"
            android:exported="true"
            android:label="@string/title_activity_dice"
            android:theme="@style/Theme.DiceCupCompulsory.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".dice.DiceStoryActivity"
            android:exported="false"
            android:label="@string/title_activity_dice_story"
            android:theme="@style/Theme.DiceCupCompulsory.NoActionBar" />
    </application>

</manifest>